using Library.Models;

namespace Console;

internal class Program
{
    static void Main(string[] args)
    {
        //Console.WriteLine("Hello, World!");
        //var ddd = new BasicTypesModel(default, default, default, default, default, default, default, default, default);
    }
}
